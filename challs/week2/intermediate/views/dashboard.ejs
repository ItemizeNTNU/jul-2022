<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="utf-8" />
		<meta http-equiv="X-UA-Compatible" content="IE=edge" />
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<title>Santa's early christmas gift | only for the nice children</title>
		<link rel="stylesheet" type="text/css" href="style.css" />
	</head>
	<body>
		<main>
			<div class="giftlist">
				<div>
					<h2>
						Hello,
						<span title="<%= user.name%>"><%= user.displayName %></span>
					</h2>
					<p>
						Your naughty or nice status for this year is currently: "<%=
						user.status %>"
					</p>
					<p class="message"><%- user.message %></p>
				</div>

				<div class="list">
					<div>
						<h3>Naughty list 😈</h3>
						<% for(let i=0; i < giftlist.length; i++) { %> <% if
						(giftlist[i].status === "naughty") { %>
						<div>
							<p title="<%= giftlist[i].name%>">
								<%= giftlist[i].displayName%> - ❌
							</p>
						</div>
						<% } %> <% } %>
					</div>

					<div>
						<h3>Nice list 👼</h3>
						<% for(let i=0; i < giftlist.length; i++) { %> <% if
						(giftlist[i].status === "nice") { %>
						<div>
							<p title="<%= giftlist[i].name%>">
								<%= giftlist[i].displayName %> - 🎁
							</p>
						</div>
						<% } %> <% } %>
					</div>
				</div>
			</div>
		</main>
	</body>
</html>
