<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="utf-8" />
		<meta http-equiv="X-UA-Compatible" content="IE=edge" />
		<meta name="viewport" content="width=device-width, initial-scale=1" />
		<title>Oreo | dashboard</title>
		<link rel="stylesheet" type="text/css" href="style.css" />
	</head>
	<body>
		<main>
			<div class="giftlist">
				<div>
					<span class="gift-head">👼</span>
					<% for (let i=0; i < giftlist["good"].length; i++) { %>
						<span><%= giftlist["good"][i]%></span>
					<% } %>
				</div>
				<div>
					<span class="gift-head">👿</span>
					<% for (let i=0; i < giftlist["bad"].length; i++) { %>
						<span><%= giftlist["bad"][i]%></span>
					<% } %>
				</div>
				<div>
					<span class="gift-head">Waiting ⏳</span>
					<% for (let i=0; i < giftlist["waiting"].length; i++) { %>
						<span><%= giftlist["waiting"][i]%></span>
					<% } %>
				</div>
			</div>
		</main>
	</body>
</html>